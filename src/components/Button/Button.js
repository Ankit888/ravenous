import React from 'react';

class Button extends React.Component {
    render () {
        return (
           <button onClick = {this.props.laura}>
          
               Touch me!
           </button>
        );
    }
}

export default Button;